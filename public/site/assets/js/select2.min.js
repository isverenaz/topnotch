/*! Select2 compatibility stub for offline mirror pages.
 * Keeps pages from breaking when the bundled Select2 build is incomplete.
 */
(function (factory) {
	if (typeof module === "object" && module.exports) {
		module.exports = factory(require("jquery"));
	} else {
		factory(window.jQuery || window.$);
	}
})(function ($) {
	if (!$ || !$.fn) {
		return;
	}

	if (!$.fn.select2) {
		$.fn.select2 = function () {
			return this;
		};
	}

	$.fn.select2.amd = $.fn.select2.amd || { define: function () {}, require: function () {} };
	return $.fn.select2;
});
